# Day 62 实验任务单：简历项目二整合——RAG 知识库 Agent（LangGraph 版）
> 📌 **状态：已推迟（2026-08-31 决定）**——加速计划提前收官于 Day61。用户看黑马 P6-07~13 觉得本任务内容过于简单，推迟到 9 月开学后的简历项目二阶段，与 9 月企业级收尾（评估体系/Docker/README/架构图）合并做。本任务单保留作参考，届时时长与拆分按 9 月每天 4-5h 重新排。
> 日期：9/2（原定）· 预计 5 小时
> 今日主题：把 Phase 4 的 RAGService + Phase 5 的 LangGraph Agent 拼成**简历项目二「AI 知识库助手」框架版**：Agent 能自主决定"要不要查知识库"（ask_kb 工具）、会用真工具（时间/天气/文件）、有跨请求记忆（checkpointer）。参考黑马 P6-07~13 项目实战抄结构。
> ⚠️ 本任务单**提前生成**（Day61 完成前写好）：前提是 Day61 LangGraph 映射已基本掌握。如果 Day61 实验 B 没跑通，先回去补，别硬上。

## 🎯 今日目标
学完今天，你要能：
1. 给 LangGraph Agent 挂一个 RAG 工具 `ask_kb`，跑通"员工手册里请年假要满足什么条件"这类问答——**打通 RAG + Agent 两个简历项目**
2. 参考黑马 P6 项目实战搭出分层骨架：配置 / 工具 / RAG 服务 / 主 Agent 分目录
3. 用 checkpointer 实现跨请求记忆，验证"我刚才记的什么"能记住（Day60 遗留问题解决）
4. 能讲清：**Agent 套 RAG vs 纯 RAG** 的区别（Agent 自主决定查不查、查完还能再调别的工具）

---

## 一、概念预习（约 1 小时）——先看课，不迷茫

第一步：看黑马课程 P6 项目实战部分（**扫一眼抄结构**，不用精看，当"项目结构参考手册"）：
- **P6-07 日志和路径工具开发**——看日志工具怎么写（生产项目必备，对应你 Day60 的 print）
- **P6-08 配置工具文件工具和提示词加载工具开发**——看配置怎么管理（对应你的 .env / 写死的模型名）
- **P6-09 向量存储服务开发**——对应你 Day52/57 的 ChromaDB
- **P6-10 RAG总结服务开发**——对应你 Day57 的 RAGService.ask
- **P6-13 用户界面开发**——可选，可换成你熟的 Streamlit（Day36-40 学过）
- ⏭ P6-11 tools 开发 / P6-05 中间件 / P6-12 中间件和Agent创建 前面已看完

第二步：**回顾你自己的 Day57**：`svc.ask(query)` 返回 `(answer, [(src_id, 原文)])`——这个接口今天要包成 @tool。

边读边想的 3 个问题（读之前先想，读完再对答案）：
1. 纯 RAG（Phase 4）和 Agent 套 RAG 有什么不同？（提示：纯 RAG 每次必查库；Agent 会判断"这问题要不要查库"，还能查完库再调天气/文件工具）
2. 为什么 ask_kb 要用 @tool 而不是直接在 agent_node 里调？（提示：只有 Agent 决定调时才执行，还能和别的工具组合成多步任务）
3. 黑马项目的目录结构（配置/工具/服务/主Agent）和你之前的单文件脚本比，工程上好在哪？（提示：可维护 / 可测试 / 团队协作 / 简历能讲"分层架构"）

---

## 二、动手实验（约 3.5 小时，核心）

### 实验 A：给 Agent 挂 RAG 工具 ask_kb【骨架】★★★

**概念**：Agent 的"知识"来自 RAG。把 Day57 的 RAGService 包成 @tool，Agent 就能按需查知识库——"Agent + RAG"合体，**两个简历项目在这一步打通**。

【骨架】（关键行留空你填，RAGService 代码从 Day57 抄）：

```python
# exp_a_agent_rag.py —— 会查知识库的 Agent
from langchain_ollama import ChatOllama
from langchain_core.tools import tool
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import add_messages
from langgraph.checkpoint.memory import MemorySaver
from typing import TypedDict, Annotated, Literal

# ① 从 Day57 抄 RAGService（或 import）——它封装了 ChromaDB+检索+LCEL chain ——
class RAGService:
    def __init__(self): ...   # 抄 Day57：建库/检索器/prompt/llm/chain
    def ask(self, query): ... # 返回 (answer, [(src, 原文)])

svc = RAGService()   # 建一次（库/模型都建好，别每个工具调用都重建）

# ② RAG 工具：把 svc.ask 包成 @tool —— 你填
@tool
def ask_kb(query: str) -> str:
    """查询公司知识库（员工手册等），返回带引用的答案。参数=问题文本。"""
    # answer, sources = svc.ask(query)
    # 把 answer + 引用原文拼成模型看得懂的文本返回
    pass

TOOLS = [ask_kb]   # ③ 再加 get_time / get_weather（从 Day60 exp_b 抄，去掉归一化）—— 你填

llm = ChatOllama(model="qwen2.5:7b", temperature=0)
llm = llm.bind_tools(TOOLS)

# ④ agent 节点 / tools 节点 / 条件路由：从 Day61 exp_b 复制 ——
# ⑤ compile 加 checkpointer=MemorySaver()，invoke 传 thread_id ——
```

**步骤**：
1. 把 Day57 的 RAGService 抄进来（或 import），建 svc 一次
2. 填 ②：把 `svc.ask` 包成 ask_kb（注意：返回值是 `(answer, [(src,原文)])` 元组，要**拼成一段人话文本**，别直接返回元组给模型——模型读不懂 Python 结构）
3. 从 Day61 exp_b 复制图的骨架，换成 `TOOLS=[ask_kb, get_time, get_weather]`，加 checkpointer
4. 跑通："员工手册里请年假要满足什么条件？"（知识库问题）→ 看 Agent 调 ask_kb → 带引用回答
5. **关键观察**：再问"现在几点了？"（非知识库问题）→ 看 Agent 会不会**不调 ask_kb**、直接用 get_time 答——这就是 **Agent 自主决定查不查库**，纯 RAG 做不到

### 实验 B：项目骨架 + 跨请求记忆【提示】★★

**概念**：单文件脚本能跑，但不是"项目"。参考黑马 P6 抄一个分层骨架，把脚本拆成模块——这就是简历上能写的"分层架构"。

**步骤**（提示级，不给代码）：
1. 建目录结构（参考黑马 P6-07/08/09/10/12 的分层思路）：

```
agent_kb_project/
├── config.py          # 配置（模型名 / base_url / 库路径，参考 P6-08）
├── tools/             # 工具目录（ask_kb / get_time / get_weather，参考 P6-11）
│   └── __init__.py    # 把 TOOLS 列表汇总导出
├── rag_service.py     # RAG 服务（Day57 抄，参考 P6-09/10）
├── graph.py           # LangGraph 图（agent 节点 / 工具节点 / 条件路由，Day61 抄）
├── main.py            # 入口：命令行循环问问题
└── logs/              # 运行日志（参考 P6-07）
```

2. 每问一个问题把 messages 存日志（或打印），用 **checkpointer 跨请求记忆**验证：问"我叫小明"→ 开第二次请求问"我叫什么" → 能答出来（同一个 thread_id）
3. 串完整流程：知识库问题 / 天气问题 / 连续对话 各测一遍，填表

### 实验 C：自测演示【提示】

**概念**：项目收官前做一轮破坏性自查——前几天的坑今天全验证一遍，确保没白学。

**步骤**（提示级，不给代码）：
1. 问一个库里没有的（"空调坏了找谁修"）→ 看 Agent 答"不知道"（Day55/57 防幻觉兜底还在吗？）
2. 问"今天适合跑步吗"（需要天气 + 知识库综合）→ 看 Agent 会不会依次调 get_weather + ask_kb（**多工具协同 + RAG 组合**，Day60 实验 B 的升级版）
3. 给 Agent 加 `delete_note` 工具（Day60 自评里你设计过）→ 验证 `_safe_path` 文件安全边界还在不在（路径穿越挡得住吗？）

### 观察记录表（跑完填）

| 实验 | 观察项 | 结果 |
| ---- | ------ | ---- |
| A | 知识库问题 → 调 ask_kb 了吗？回答带引用吗？ | |
| A | "现在几点了" → 会跳过 ask_kb 吗？ | |
| B | 跨请求记忆"我叫什么"记得吗？ | |
| C | 库外问题答"不知道"吗？ | |
| C | 综合问题依次调了几个工具？顺序？ | |

---

## 三、自测 5 关（晚上自查）

- **能讲**：Agent 套 RAG 和纯 RAG 的区别？ask_kb 为什么用 @tool？（面试必问：**RAG 和 Agent 怎么结合**）
- **能写**：默写"给 Agent 挂 RAG 工具"三步（svc.ask 包 @tool → bind_tools → 图加工具节点）
- **能修**：Agent 答"没有这个工具" → 查什么？（提示：TOOLS 列表 和 bind_tools(TOOLS) 一致性）
- **能答 why**：为什么项目要拆模块而不是单文件？（提示：可维护 / 可测试 / 黑马生产结构 / 简历有料）
- **能扛边界**：知识库 10 万文档 + Agent 长期记忆 → 什么先撑不住？（提示：RAG 检索 + checkpointer 状态持久化）

---

## 四、今日产出（打卡用）
1. 学习笔记《简历项目二整合：RAG 知识库 Agent》补进 `学习笔记.md`（模板：大白话 / 工程·面试视角 / ❌易错 / 面试常考附参考答案）
2. `agent_kb_project/` 项目骨架 + 实验表格填好 + 跑通截图
3. 概念地图 **Phase 5 第4行**「记忆（checkpointer）/ 防死循环」→ 回填 ✅（第3行 LangGraph 昨天 Day61 已回填）
4. git commit（提交前 `git status` 确认 `.env`、`chroma_data*`、`notes/` 该 ignore 的没混进来）

---

## 五、给学习助手的自评
- "Agent 套 RAG"这个合体，你觉得自己讲得清吗？（面试高频：RAG 和 Agent 怎么结合？）
- 拆目录后和单文件比，工程上差别在哪？能对着简历说"分层架构"吗？
- 项目二现在到"能跑 + 框架版"了吗？还缺什么？（评估 / Docker / README / 架构图 → 9 月企业级收尾）

> 📌 阶段预告：加速计划 Phase 5 到此收官！9/3 开学后进入企业级收尾——把两个简历项目补评估体系 / Docker / README / 架构图 / 博客，打磨成能面试讲的完整作品。届时你每天 4-5h，按 9 月收尾任务单走。
