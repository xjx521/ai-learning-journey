# Day 49 实验任务单：Phase 3 阶段复习 + 面试冲刺

> 日期：8/20 · 预计 5 小时
> 今日主题：**Day49 = Phase 3 阶段复习日**（对齐 Day42 的复习模式）。把 Day43-48 六个概念串成一条线，查漏补缺，学完新补的「面试冲刺 10 考点」，练到"不看笔记也能答"。

---

## 🎯 今日目标
学完今天，你要能：
1. **不看笔记**，把 Phase 3 全部概念用大白话讲出来（能讲关——复习的核心验收）
2. 答出新增「面试冲刺 10 考点」里的题（至少 7/10 不卡壳）
3. 产出一份「我不会清单」——把复习中讲不出的、答不上的全部标红，之后每天对着补

---

## 一、复习路线（上午 1.5 小时）——先看材料，不是重学，是"回顾+找漏"

### 第一步：对着概念地图扫一遍（30 分钟）
打开 `第三~第五阶段加速学习/概念地图.md` 的 **Phase 3 表**（现在有 17 行 ✅）：
- 每行「一句话解释」盖住，**先自己讲一遍**再对照
- 讲不出的那行，就是今天的重点复习对象 → 记进「我不会清单」

### 第二步：读新增的「面试冲刺 10 考点」（40 分钟）
`学习笔记.md` 尾部新增了 **`## 🎯 Phase 3 面试冲刺：LLM 高频考点补充`**，共 10 节：
> 补充1 Transformer/自注意力 → 补充2 预训练/SFT/RLHF → 补充3 解码策略 → 补充4 KV Cache → 补充5 Prompt Caching → 补充6 RAG vs 微调 vs 长上下文 → 补充7 模型评估 → 补充8 模型选型 → 补充9 Embedding（Phase 4 预告）→ 补充10 Agent（Phase 5 预告）

**🚫 不要背**，只看懂 + 能用自己的话复述。这 10 节是**面试常考但你还没学过**的，属于"知道就有分、不知道就哑巴"的题。

### 第三步：回看 Day43-48 笔记的关键实验结论（20 分钟）
重点回看 Day47 的 4 个 finish_reason、滑动窗口删法；Day48 的会话淘汰 LRU 三件套。这两块是"动手做过 + 面试能讲细节"的加分点。

### 边读边想的 3 个问题
1. 概念地图 17 行里，有几行你能**不看笔记讲满 1 分钟**？少于 10 行说明有漏。
2. 补充 6（RAG vs 微调 vs 长上下文）为什么是"面试必考"？它和你 Day48 项目什么关系？
3. 如果你现在去面试，被问"讲一下你们的会话淘汰怎么做的"，你能讲出几步？（提示：登记→刷新→超限→min→删DB→删字典→commit，数一数自己答得出几步）

---

## 二、动手复习（下午 3 小时，核心）——不是做题，是"模拟输出"

### 实验 A：概念地图过 5 关（1.5 小时）
对 Phase 3 表**每一行**，走一遍 5 关自查（学习指南第三节的深度 5 关）：
| 关       | 怎么做                                                                                            |
| -------- | ------------------------------------------------------------------------------------------------- |
| 能讲     | 盖住笔记，用大白话讲给"空气"听（可以假装教同学）                                                  |
| 能写     | 白纸写这段代码的最小骨架（如：manage_history 的 del messages[1:3]、remember_activity 的 min+del） |
| 能修     | 想一个这概念最常见的坑，说怎么定位（如：多轮失忆→查是不是没带历史）                               |
| 能答 why | 自己问"为什么这么设计"，答得上来吗                                                                |
| 能扛边界 | 空输入/超长/并发/断网，会崩在哪                                                                   |

**记录**：每行过完打个 ✓；过不去的写进「我不会清单」。

### 实验 B：面试题自测（1 小时）
从笔记里抽题，**口头默答**（不要写，模拟面试）：

抽 5 道 Day43-48 的题 + 5 道新补充的题：
1. LLM 怎么生成一段话？为什么说它不理解？
   根据提示词要求依靠模型训练数据逐个预测下一个token 模型并不理解意思增长只是跟随训练数据预测
2. temperature 和 top_p 区别？事实抽取用高还是低？
   事实抽取选用低 temperature直接影响模型下一个预测的token 越高下一个随机性越大适合创作 而top_p则是自上而下依靠训练数据的概率相加直到超过p值就选前几个token作为下一个
3. 多轮对话为什么无状态？怎么实现记忆？
   因为模型没有记忆 所有记忆都是依靠每次请求把上下文对话历史一起传给模型
4. Function Calling 三段式？为什么 arguments 是字符串？
   
5. 上下文窗口满了怎么办？为什么删 user 要成对删？
   上下文窗口满了我会优先删除最旧最久未活跃 因为每个aisstant的回答都是基于他前面的user提问进行回答的如果只删除其中一个user信息会导致结构混乱模型易出现幻觉单独
6. （新）自注意力 QKV 是什么？
   Q是查询 K是键 V是值
7. （新）预训练/SFT/RLHF 各解决什么？
   预训练解决的是
8. （新）KV Cache 是什么？为什么长上下文吃显存？
9.  （新）RAG vs 微调 vs 长上下文怎么选？
    频繁更新知识库/私有数据/可溯源选RAG 让模型真正学习专业术语/输出固定风格知识选微调 单次分析超长文本选长上下文
10. （新）生产怎么降 LLM 成本？（至少 3 条）
    1.简单任务选用便宜轻量速度快的小模型 2.
每题答 1-2 分钟。**卡壳的标红进清单**。

### 实验 C：代码复现（30 分钟）
不看笔记，重写这三个函数的**骨架**（写在纸上或注释里，不用真跑）：
1. `manage_history(messages, limit)` —— 超限怎么删、保底几行
   while count_token(messages)>limit:
        if len(messages)>5:
            break
        del messages[1:3]
    return messages
2. `remember_activity(session_id, db)` —— 淘汰三件套
session_activity:Dict[str,float]={}#×忘记初始化
MAX_SEESIONS=10#最多初始化
    session_activity[session_id]=time.time()
    if len(session_acticity)>MAX_SESSIONS:#×应该判断session条数不是判断token
        oldest=min(session_avtivity,key=session_activity.get)
        stmt=select(ChatMessage).where(ChatMessage).where(ChatMessage.session_id==oldest)
        result=await db.excute(stmt).sclars().all()
        for row in result :
            await db.delete(row)#没有先查询再一条条删除数据库
        del session_activity[oldest]
3. `get_llm_answer` 的骨架 —— 重建历史→system首轮加→调LLM→落库
    await remember_activity(session_id,db)
    db.commit()
    stmt=(select(ChatMessage).where(ChatMessage.session_id==session_id).order_by(ChatMessage.id))
    message=await db.excute(stmt).sclars().all()
    system_prompt={"role":"system","content":""}
    if not history or history[0]["role"]!="system":
        history.insert(0,system_prompt)
        system_text=ChatMessage(session_id=session-id，role="system",content=system_prompt)
        db.add(system_text)
    history=[{"role":m.role,"content":m.content}for m in message]
    history.append({"role":"user","content":text})
    history=manage_history(history,limit)
    response=await client.chat.completions.create(message=history)#×忘记 await client.chat.completions.create
    history.append("role":"assistant","content":response.choice[0].message.content)
    history=manage_history(history,limit)

    user_input=ChatMessage(
        session_id=session_id,role="user",content=text
    )
    ai_output=ChatMessage(session_id=session_id,role="assistant",content=response.choices[0].message.content)
    db.add(user_input)
    db.add(ai_output)
    await db.commit()
    return response.choice[0].message.content
写完和 `backend/main.py` 对照，**标出写错的每一处**。

---

## 三、自测 5 关（晚上 30 分钟）
- **能讲**：完整讲一遍「一条消息从前端到落库的旅程」（Day48 第六节）
- 前端收到session_id和提问->传到后端收到session_id刷新本轮会话判断是否超过会话最大数要删除落库->判断是否需要传入system并add->传入提问到history->判断是否超过上下文窗口限制超过立即截断->调用LLM获取ai回答->判断是否超过上下文窗口限制超过立即截断->提交落库
- **能写**：默写 LRU 淘汰三件套 + 一句"为什么先刷新当前会话"
  - 避免当前对话被当作最久未活跃对话删除
- **能修**：前端问问题 500 报错，去哪看日志？`InvalidApiKey` 怎么改？
  - 去控制台看日志 检查APIKEY是否合法
- **能答 why**：为什么长对话成本涨得比打字量快？为什么 RAG 适合私有知识库？（结合补充 6）
  - 因为每次对话都会一起把上下文历史一次性全部传入长对话成本涨得快 因为RAG会把历史对话检索相关知识拼入prompt
- **能扛边界**：10 个用户同时提问，你的后端会卡吗？为什么？
    不会采用了异步AsyncOPAI 可以同时在一个用户提问等待时执行其他进程避免一个用户提问所有用户一起等待的同步阻塞
---

## 四、今日产出（打卡用）
1. **「我不会清单」**：一份标红清单（讲不出的概念 + 答不上的面试题），这是今天最重要的产出
2. 概念地图确认：17 行全部能过 5 关
3. git commit（先 `git status` 确认无 `.env`）

## 五、给学习助手的自评
- 今天 5 关里，哪一关最吃力？（上几次都是"能写"，这次还是吗？）
- 是的还有新补充考点 只看题目有点懵
- 新增的 10 个补充考点里，哪个最没底？要不要我再展开讲讲？
- QKV Prompt Caching KV Cache
- 「我不会清单」里你最想先补哪 3 个？
写在上面了
---

> 📌 复习提示：Day49 的重点不是"再看一遍笔记"，是**合上笔记往外倒**。能倒出来 = 会了；倒不出来 = 欠债，记进清单。清单会跟着你直到 12 月面试。
