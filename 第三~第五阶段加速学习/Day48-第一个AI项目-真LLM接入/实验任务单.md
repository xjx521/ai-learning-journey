# Day 48 实验任务单：第一个 AI 项目——把 mock LLM 换成真 DeepSeek

> 日期：8/16 · 预计 5 小时
> 今日主题：把 Day39-40「AI 应用模板」里的关键词假回答，换成真 LLM（DeepSeek），前端一行不改。这是 Phase 3 的"验收"——前 5 天学的 API/多轮/上下文管理，今天全用上。

---

## 🎯 今日目标
学完今天，你要能：
1. 说出"前后端分离"为什么能让你**只改后端一个函数**就换掉整个 AI 大脑
2. 后端 `/api/chat` 从"关键词假回答"变成"真 DeepSeek 回答"，前端跑起来直接能聊
3. 给后端加"多轮记忆"（历史存哪、超窗怎么管），并打印每轮 token 和估算成本

> 知识钩子：Day43 你学会了调 API，Day44 学会了带历史多轮，Day47 学会了 manage_history 管窗口。今天是第一次"组装成真项目"。
> ⚠️ 项目路径：`第二阶段/Week6-前端基础与Streamlit/Day39-40-综合项目-AI应用开发模板/`（别在别的文件夹另起炉灶，就在原项目上改）

---

## 一、概念预习（约 1 小时）——先看课，不迷茫

### 第一步：看吴恩达课程（约 30 分钟）
- Part 2 **集 10（在软件应用中使用生成 AI）**——看它怎么把一个 LLM 嵌进真实 App（前后端分工）
- 🚫 跳过：集 11 训练 / 集 14 RAG（那是 Phase 4 的菜）

### 第二步：回看自己 Day43 的 `call_deepseek.py`（约 30 分钟）
- 不新查文档，就看你当时是怎么调通的：`OpenAI(api_key, base_url)` → `.chat.completions.create(model, messages)` → `.choices[0].message.content`
- 今天唯一的新问题：**那段代码在"独立脚本"里能跑，塞进 FastAPI 的路由函数里要注意什么？**（提示：路由函数是 `async def`，而 openai 调用是同步阻塞的——想想会不会卡住其他请求，先自己猜）

### 边读边想的 3 个问题
1. 前端 Streamlit 现在调的是 `POST /api/chat`，它关心后端用什么模型吗？为什么 Day39-40 要故意做成"前后端分离"？
2. 如果后端同时来了 10 个用户的聊天请求，每个都在等 DeepSeek 慢慢回复，会有什么问题？（这就是"同步阻塞 vs 异步"）
3. 换真 LLM 后，**回答变慢**（从几毫秒变成几秒），前端要不要做点什么让用户不等得干着急？

---

## 二、动手实验（约 3.5 小时，核心）

> 前置：先确认后端能启动。进 `backend/` 目录，`.env` 有没有？（Day35 教过：没有就复制 `.env.example` 或手建）。数据库连接如果起不来，**先别管数据库**——我们今天只动 `/api/chat` 接口，其他接口能不动就不动。

### 实验 A：后端换真 LLM（约 1.5 小时）★★★ 核心

任务：把 `backend/main.py` 里 `get_llm_answer` 这个关键词函数，换成真 DeepSeek 调用。

> 调 API 的写法 Day43 已经给过【示例】了，所以今天只给【骨架】+【提示】，你写，我不代写。

【骨架】填空（你填 TODO 部分）：

```python
# backend/main.py —— 顶部 import 区（在现有 import 后面加）
import os
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()  # 读 .env
client = OpenAI(
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1",
)

# —— 把原来的 get_llm_answer 整段替换掉 ——
def get_llm_answer(text: str) -> str:
    """真 LLM 回答。text 是用户输入，返回模型回复。"""
    # TODO 1: 构造 messages，只有一个 user 消息，content 就是 text
    # TODO 2: client.chat.completions.create(...)，参考 Day43 的 call_deepseek.py
    # TODO 3: 返回 .choices[0].message.content
    ...
```

【提示】具体要注意的三处：
1. `.env` 文件：在**项目根目录**（`Day39-40-.../` 下，和 backend 平级）建 `.env`，内容 `DEEPSEEK_API_KEY=你的key`（从 Day47 的 `.env` 复制过来，别重新粘贴弄错）。**⚠️ 确认 `.gitignore` 里有 `.env`**，提交前会再扫一遍。
2. `get_llm_answer` 现在是个**同步函数**，路由 `get_llm_chat` 是 `async def`——先按同步调，跑通再说。真正生产要换成 `asyncio.to_thread(...)` 或异步客户端，**你今天能说出"为什么同步会卡"就算过关**（实验观察题里写）。
3. 改完跑 `uvicorn main:app`，用浏览器开 `http://localhost:8000/docs`，试 `POST /api/chat`，传 `{"text": "你好"}` 和 `{"text": "给我写一首关于大海的诗"}`。

填对比表：

| 测试问题 | mock 时期会返回 | 真 LLM 返回 | 关键差异 |
| -------- | --------------- | ----------- | -------- |
| 你好 | 固定"你好！我是你的 AI 助手…" | | |
| 天气 | "我现在还是模拟回答…" | | |
| 给我写一首关于大海的诗 | 兜底"你问的是，【…】" | | |

- **观察思考**：真 LLM 回答和你 Day43 单独脚本跑，代码量差多少？为什么"前端一行没改"也能用？

### 实验 B：给后端加"多轮记忆"（约 1 小时）★★

任务：让后端记住同一会话的历史——不是每次都只发一句话，而是把历史一起发过去（Day44 原理 + Day47 的 manage_history 管理）。

> 不用做登录/用户体系，用一个简单的内存字典模拟"一个会话"：`session_history = {}`，key 是会话 id，value 是 messages 列表。够演示原理就行。

【骨架】填空：

```python
# backend/main.py —— 加一个全局变量 + 改 get_llm_answer 函数
from typing import Dict, List

session_history: Dict[str, List[dict]] = {}   # 会话id -> messages 列表

def get_llm_answer(text: str, session_id: str = "default") -> str:
    history = session_history.setdefault(session_id, [])
    history.append({"role": "user", "content": text})
    # TODO 1: 用 Day47 的 count_tokens / manage_history 思路，
    #         在发请求前检查 history 总 token 超没超上限（比如 800）
    # TODO 2: 调 DeepSeek（把 history 整个作为 messages 发过去）
    # TODO 3: 把模型的回答 append 进 history（role="assistant"）
    # TODO 4: 返回模型回答
    ...
```

【提示】：
- 路由里怎么拿到 session_id？最简单：`POST /api/chat` 请求体里加一个可选字段 `session_id`（改一下 `ChatRequest` 模型，default 给 "default"）。前端不发就用默认会话。
- **验证"记住了"**：第一轮问"我叫小明"，第二轮问"我叫什么名字？"——看它答不答得出。
- **验证"窗口管理"**：把 limit 调小（比如 100），多聊几轮，看它怎么删最旧历史（可以打印 `len(history)` 观察）。

填记录表（第 1 轮 → 第 5 轮各记一行）：

| 轮数 | history 条数 | history 总 token（估算） | 超 limit 吗 | manage 后剩几条 | 回答还认得"小明"吗 |
| ---- | ------------ | ------------------------ | ----------- | --------------- | ------------------ |
| 1    |             |                          |             |                 |                    |
| 2    |             |                          |             |                 |                    |
| 3    |             |                          |             |                 |                    |
| 5    |             |                          |             |                 |                    |

### 实验 C：成本观察 + 前端体验（约 1 小时）★

任务：①在 get_llm_answer 里打印每次请求的 token 和估算成本；②启动 Streamlit 前端，真问几个问题看效果。

【提示】：
1. **成本打印**：调用前算 `input_tokens = count_messages_tokens(history)`，调用后算输出 token（`response.usage` 里有官方数值，`prompt_tokens` / `completion_tokens`，先打印看看）。估算成本 = `输入token×输入单价 + 输出token×输出单价`（Day47 你查过 DeepSeek 单价）。
2. **前端**：`cd frontend && streamlit run app.py`，浏览器打开，问"你好"、"写首诗"、连续追问几句，观察：
   - 真 LLM 回答和 mock 比，变慢了多少？
   - 前端有没有"加载中"的提示？（Day39-40 是 `st.spinner` 还是裸等？）
   - 多轮追问时，后端因为实验 B 有了记忆，前端体验是不是更连贯了？

填对比表：

| 观察点 | mock 时期 | 真 LLM 后 |
| ------ | --------- | --------- |
| 回答速度 | 瞬间 | |
| 回答质量（天气/写诗） | 固定话术 | |
| 多轮连贯性 | 每轮都独立 | |
| 前端要不要改代码 | 不用 | |

- **观察思考**：为什么真实产品里，LLM 应用"慢"是常态？前端能做什么掩盖延迟？（提示：流式输出 Day44 学过，打字机效果）

---

## 三、自测 5 关（晚上自查）
- **能讲**：一句话说清"前后端分离"怎么让你今天只改了一个函数就换掉了 AI 大脑？
- **能写**：关掉资料，写一个"带历史的 get_llm_answer"伪代码（不用真跑，逻辑对就行：历史怎么存、怎么带、怎么管窗口）
- **能修**：前端问问题报 `500 Internal Server Error`，你第一步去后端哪里看日志？报错里出现 `InvalidApiKey`，怎么改？
- **能答 why**：为什么实验 A 说"同步阻塞"是问题？10 个用户同时问，同步版会发生什么？`asyncio.to_thread` 是干嘛的？
- **能扛边界**：用户连续问 50 轮，后端内存里 history 无限涨会怎样？除了"删最旧"，还有什么更稳的方案（提示：Session 过期清理 / 存数据库）？

## 四、今日产出（打卡用）
1. 跑通：后端 `/api/chat` 真 LLM 回答截图 + 前端 Streamlit 对话截图
2. 实验 A/B/C 三张对比表（上面都填好）
3. 项目代码 diff（改了几个文件、每处改了什么）
4. 学习笔记《第一个 AI 项目》+ 概念地图回填（可回填「前后端分离接入 LLM」概念）
5. git commit（**提交前 git status 扫一眼，确认 .env 没混进来**）

## 五、给学习助手的自评（交给我时附上，⚠️ 前几次都记得填了，这次也别忘了）
- 今天最吃力的是哪一步？是 API 接入、历史管理，还是前端联调？
- 用你自己的话解释：为什么"前后端分离"让换模型变得这么容易？代价是什么（前端要等、接口要定好）？
