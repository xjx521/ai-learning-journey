# Day48 交接文档（2026-08-17 开新对话用）

> 给新对话助手的开场白：用户在做 **Day48 第一个 AI 项目（把 mock LLM 换成真 DeepSeek）**，中途开了新对话，读这份文档接手。用户是基础薄弱的学生，教学原则见 `D:\Go work\CLAUDE.md`：**引导优先、不代写代码、代码带注释、中文解释**。当前是实验 B（多轮记忆）中途，代码已改大部分但**有 2 处 bug 待修**。

---

## 一、当前进度：Day48 第一个 AI 项目（Phase 3 最后一个验收日）

用户背景速览：软件工程准大四，2026-12 找 AI 应用开发实习，8/31 开学前学完 Phase 3-5。今天把 Day39-40「AI 应用模板」的 mock LLM 换成真 DeepSeek，前端一行不改。

### 已完成 ✅
1. **实验 A（换真 LLM）**：`backend/main.py` 用 `AsyncOpenAI`（api_key + base_url=https://api.deepseek.com/v1, model=deepseek-v4-flash）替换了关键词假回答。对比表已填（你好/天气/写诗）。
2. **实验 B 核心代码已改**：`get_llm_answer` 从"无状态单轮"改成了"带多轮记忆 + 存数据库"：
   - 从数据库查历史（`select(ChatMessage).where(session_id==...).order_by(id)`）→ 拼成 `history` dict 列表
   - system 只在首轮加（判断 `history[0]["role"] != "system"`）
   - 用 Day47 的 `count_tokens` / `manage_history` 管理上下文窗口（limit=1000）
   - 调 LLM → 把 user + assistant 两条新消息写回数据库
   - **架构决策（用户已选定）**：内存管会话 + 数据库管持久，两层配合（终极方案选存数据库）
3. **数据库 & 迁移已修好**：
   - alembic 迁移链：`de16b71cab7a`（prompts）→ `9d3646f1700c`（chatmessages），已 `upgrade head`
   - chatmessages 表结构正确：id 自增主键 / session_id 带索引 / role / content / 时间戳
   - 数据库当前 **chatmessages 表为空**（刚清空过测试数据，可重新验证）

### ⚠️ 待修 Bug（当前卡点，最重要）
**`get_llm_answer` 里两处 ChatMessage 的 role 写反了**（复制粘贴调包）：
- `main.py` ~148 行：`system_text = ChatMessage(session_id=..., role="assistant", content=system_prompt)` —— 存的是系统提示词，**role 应为 `"system"`**
- `main.py` ~167 行：`ai_text = ChatMessage(session_id=..., role="system", content=response...)` —— 存的是模型回答，**role 应为 `"assistant"`**

后果：数据库里 system 提示词被标成 assistant、模型回答被标成 system。这会让 137 行 `history[0]["role"] != "system"` 判断永远成立 → **system 每轮重复添加**，记忆逻辑崩。

修法：**用户自己改这两行 role 互换**（不代写），改完重启 uvicorn，跑 2 轮对话验证。

### ⬜ 未完成
1. **修上述 2 处 role bug + 验证**（改 role → 重启 → 对话 2 轮 → 查库确认 system 只有 1 条且 role 正确）
2. **会话淘汰（会话数量上限）**：用户方案里说"内存管会话 + 数据库管持久，两层配合"，但**会话数量上限逻辑还没写**——当前内存 `session_history` 字典已删（改走数据库了），需要设计：比如一个 dict 记录 session_id → 最近活跃时间，超 MAX_SESSIONS 就淘汰最久未活跃的会话（删 DB 行 + 删注册），或简化版
3. **实验 C（成本观察 + 前端）**：get_llm_answer 里打印每轮 token 和估算成本；启动 Streamlit 前端联调看真 LLM 效果。对比表未填。
4. **实验 A/B/C 记录表**：任务单里有几张对比表，部分已填（实验 A 填了），实验 B/C 的表格待填
5. **自测 5 关**（能讲/能写/能修/能答 why/能扛边界）+ **给学习助手的自评**（任务单第五节，用户之前每次都填）
6. **学习笔记《第一个 AI 项目》** 补入 `第三~第五阶段加速学习/学习笔记.md`（模板：大白话/工程视角/❌易错/面试题）
7. **概念地图回填**：`概念地图.md` Phase 3 表加「前后端分离接入 LLM」行
8. **git commit**：提交代码（⚠️ 先 `git status` 确认 `.env` 没混入，.gitignore 已忽略但仍要扫一眼）；**git push 之前因网络失败未推，需手动重试**

---

## 二、环境信息（新对话可能用到）

| 项 | 值 |
|----|-----|
| venv | `D:\Go work\venv`（重建过，Python 3.14.6，79 个包全装好） |
| 后端启动 | `cd backend && uvicorn main:app --port 8000 --reload`（venv 内） |
| 数据库 | MySQL `ai_app`，异步引擎，连接串在 `.env` 的 `ASYNC_DATABASE_URL` |
| 前端 | `cd frontend && streamlit run app.py` |
| API | DeepSeek（openai 库 + base_url=api.deepseek.com/v1），Key 在 `.env` |

⚠️ **venv 千万别再移动**——上次移动后所有 .exe launcher 全废（硬编码旧路径），重建花了很久。要操作 alembic 用：`"/d/Go work/venv/Scripts/alembic.exe"`，python 用 `"/d/Go work/venv/Scripts/python.exe"`。

---

## 三、技术要点（接手人速记）

- **`get_llm_answer` 已重构为普通函数**（不再用 `Depends(get_db)`），路由 `get_llm_chat` 注入 db 后 `await get_llm_answer(db=db, ...)` 传入——这个已修对，别改回去
- 存库只用 `db.add()` 登记 + 最后 `await db.commit()` 一次提交（system 首轮 + user + assistant 一次 commit）
- `db.add()` 是同步的不用 await，`db.commit()`/`db.execute()` 要 await
- 历史从数据库重建（134 行 dict 推导）——这是"存数据库管持久"的落地，删错 role 就是这个机制的命门
- Day47 遗留：`manage_history` 是 `del messages[1:3]` 成对删（保 system），`count_tokens` 中文×1.5+英文×1.3+符号×1

---

## 四、给新助手的第一个动作

用户会在新对话里说"继续 Day48"之类的话。接手后：
1. 先读 `实验任务单.md`（了解今日全貌）+ `backend/main.py`（看当前代码）
2. **先修那 2 处 role bug**（引导用户自己改，不代写）→ 重启 → 跑 2 轮 → 查库验证 system 只 1 条
3. 再按上面「未完成」清单逐项推进（会话淘汰 → 实验 C → 记录表 → 自测 → 笔记 → 概念地图 → commit/push）
