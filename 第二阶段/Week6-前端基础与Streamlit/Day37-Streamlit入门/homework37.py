"""
Day 37 练习题：Streamlit 入门
============================

⚠️ 前置准备：
    pip install streamlit pandas requests

💡 学习方式：
    每个实验都对照 学习笔记.md。把下面代码里留白的 ____ 补全（自己填！），
    存成 app.py 用 `streamlit run app.py` 运行，看浏览器效果。

🛠 关于「留白」：
    只有与"当天核心知识"相关的关键行留了 ____，其余代码给全。
    每题开头有一行 💡 提示。先自己填，再翻文件最底部的参考答案。

⚠️ 重点提醒：参考答案在【文件最底部】且隔开了大量空行，做完再看！
"""

import streamlit as st  # 已导入，直接用

# ============================================================
# 【实验 1】第一个 Streamlit 应用：文本与标题
# ============================================================
"""
目标：展示标题、段落、Markdown

💡 提示：大标题用 st.title，普通文本用 st.write，富文本用 st.markdown。

把下面三行补全：
"""
st._title___("我的第一个 Streamlit 应用")  # 【填空 1】大标题
st._write___("这是用 Python 写出来的网页！")  # 【填空 2】普通文本
st._markdown___("**加粗** 和 *斜体*")  # 【填空 3】Markdown 富文本

# 📝 测试 1.1：运行后，页面顶部出现什么？三个组件分别显示什么效果？
# 答：_________出现我的第一个 Streamlit 应用 wirte普通文本形式 markdown加粗和斜体_______________________________________________

# 📝 想一想：st.markdown 里写 "## 小标题" 会怎样？(可试试)
# 答：_______________会出现一个小标题_________________________________________


# ============================================================
# 【实验 2】文本输入交互
# ============================================================
"""
目标：st.text_input 收集用户输入，并回显

💡 提示：文本框用 st.text_input，返回用户输入的字符串；用 f-string 拼到输出里。

name = st.__text_input__("你的名字", placeholder="请输入名字")   # 【填空 4】文本框
st._write___(f"你好，{name}！")                            # 【填空 5】把输入显示出来
"""
name = st._text_input___("你的名字", placeholder="请输入名字")  # 填空 4
st._write___(f"你好，{name}！")  # 填空 5

# 📝 测试 2.1：在输入框里打字，页面上的"你好"会实时变化吗？为什么？
# 答：__________会 因为每次交互都会从头到尾执行一遍代码______________________________________________
# 💡 提示：想想"每次交互脚本重跑"的原理。

# ❓ 问题 2.2：如果输入框为空，name 的值是什么？f-string 会显示什么？
# 答：__________________为空 会显示你好， ______________________________________


# ============================================================
# 【实验 3】滑块 / 下拉框 / 复选框
# ============================================================
"""
目标：用三个控件收集不同类型输入

💡 提示：滑块 st.slider(标签, min, max, value)；下拉 st.selectbox(标签, 列表)；
        复选框 st.checkbox(标签)。

age = st._slider___("年龄", min_value=0, max_value=100, value=18)   # 【填空 6】滑块
city = st._selectbox___("城市", ["北京", "上海", "广州"])              # 【填空 7】下拉框
agree = st._checkbox___("我同意条款")                                # 【填空 8】复选框
st.write(f"你 {age} 岁，在 {city}，同意={agree}")
"""
age = st.__slider__("年龄", min_value=0, max_value=100, value=18)  # 填空 6
city = st._selectbox___("城市", ["北京", "上海", "广州"])  # 填空 7
agree = st._checkbox___("我同意条款")  # 填空 8
st.write(f"你 {age} 岁，在 {city}，同意={agree}")

# 📝 测试 3.1：三个控件分别返回什么类型的值？
# 答：age 是__int__，city 是_str___，agree 是_bool___。
# 💡 提示：数字 / 字符串 / 布尔。

# ❓ 问题 3.2：把 slider 的 value=18 改成 value=50，默认值会变吗？
# 答：__________会变成50______________________________________________


# ============================================================
# 【实验 4】反馈消息：成功 / 提示 / 警告 / 错误
# ============================================================
"""
目标：用不同颜色给用户反馈

💡 提示：st.success / st.info / st.warning / st.error

score = 42
if score >= 60:
    st.__success__("恭喜，及格了！")      # 【填空 9】绿色成功
elif score >= 40:
    st._warning___("注意：接近不及格")   # 【填空 10】黄色警告
else:
    st.__error__("不及格，需要补考")   # 【填空 11】红色错误
"""
score = 42
if score >= 60:
    st._success___("恭喜，及格了！")  # 填空 9
elif score >= 40:
    st._warning___("注意：接近不及格")  # 填空 10
else:
    st._error___("不及格，需要补考")  # 填空 11

# 📝 测试 4.1：把 score 改成 80，页面显示什么颜色？改成 30 呢？
# 答：80 → _绿色___；30 → __红色__。


# ============================================================
# 【实验 5】展示数据：表格 / JSON
# ============================================================
"""
目标：展示结构化数据（AI 应用常要展示结果表）

💡 提示：可交互表格用 st.dataframe；JSON 用 st.json。

import pandas as pd
df = pd.DataFrame({
    "姓名": ["张三", "李四", "王五"],
    "分数": [90, 85, 88],
})
st._dataframe___(df)          # 【填空 12】可交互表格（可排序）
st.__json__({"result": "模型输出", "time": 0.3})   # 【填空 13】JSON 展示
"""
import pandas as pd

df = pd.DataFrame(
    {
        "姓名": ["张三", "李四", "王五"],
        "分数": [90, 85, 88],
    }
)
st._dataframe___(df)  # 填空 12
st.__json__({"result": "模型输出", "time": 0.3})  # 填空 13

# ❓ 问题 5.1：st.dataframe 和 st.table 有什么区别？
# 答：_____________dataframe可以交互如排序等 table是静态表格___________________________________________
# 💡 提示：一个可交互，一个静态。


# ============================================================
# 【实验 6】结合 FastAPI：让 Streamlit 调后端接口
# ============================================================
"""
目标：前端 Streamlit 调后端 FastAPI（这是下周项目的核心雏形）

💡 提示：用 requests.post 发请求，json= 传数据，resp.json() 取返回。

需要先启动一个 FastAPI 后端（Day 28 的 todo API 或本地手写一个 /api/chat）。
这里假设后端在 http://localhost:8000，有一个 POST /api/chat 接口，
接收 {"text": "..."}，返回 {"answer": "..."}。

import requests

API = "http://localhost:8000"
question = st.__text_input__("问点什么：")            # 【填空 14】文本框
if st._button___("提问"):                        # 【填空 15】按钮
    resp = requests._post___(f"{API}/api/chat", json={"text": question})  # 【填空 16】POST
    result = resp.json____()                    # 【填空 17】解析 JSON
    st._success___(result.get("answer", "（无回答）"))   # 【填空 18】成功绿色展示
"""
import requests

API = "http://localhost:8000"
question = st.__text_input__("问点什么：")  # 填空 14
if st._button___("提问"):  # 填空 15
    resp = requests._post___(f"{API}/api/chat", json={"text": question})  # 填空 16
    result = resp._json___()  # 填空 17
    st._success___(result.get("answer", "（无回答）"))  # 填空 18

# 📝 测试 6.1：如果没有启动后端，点"提问"会报什么错？怎么避免？
# 答：_________requests.exceptions.ConnectionError 报错无法链接_______________________________________________
# 💡 提示：连接被拒绝 / 加个 try-except。

# 🔥 挑战 6.2（重要）：写一个极简的 FastAPI 后端 /api/chat，返回固定回答，
#     再用上面的 Streamlit 调通它。这一步是 Day39-40 项目的预演！
# 答：___________代码在service.py___________________（动手做，敲代码）


# ============================================================
# 💡 参考答案（完成所有练习后再看！注意这一行下面空了很多行）
# ============================================================


"""


（参考答案在下面，先别急着翻）




实验 1 参考答案
--------------
填空 1: st.title
填空 2: st.write
填空 3: st.markdown
测试 1.1: 页面顶部出现大标题"我的第一个 Streamlit 应用"；st.write 显示普通文本；st.markdown 显示加粗和斜体。
想一想: st.markdown 里写 "## 小标题" 会渲染成二级标题（Markdown 语法生效）。


实验 2 参考答案
--------------
填空 4: st.text_input
填空 5: st.write
测试 2.1: 会。因为每次输入（交互）Streamlit 都重跑整个脚本，输入的 name 会被重新读取并渲染。
问题 2.2: 为空时 name 是空字符串 ""，f-string 显示 "你好，！"（没有名字）。


实验 3 参考答案
--------------
填空 6: st.slider
填空 7: st.selectbox
填空 8: st.checkbox
测试 3.1: age 是数字（int），city 是字符串（str），agree 是布尔（bool）。
问题 3.2: 会，默认滑块初始位置变成 50。


实验 4 参考答案
--------------
填空 9: st.success
填空 10: st.warning
填空 11: st.error
测试 4.1: 80 → 绿色（success）；30 → 红色（error）。


实验 5 参考答案
--------------
填空 12: st.dataframe
填空 13: st.json
问题 5.1: st.dataframe 可交互（可排序、滚动），st.table 是静态表格。

实验 6 参考答案
--------------
填空 14: st.text_input
填空 15: st.button
填空 16: requests.post
填空 17: .json()
填空 18: st.success
测试 6.1: 会报连接错误（ConnectionRefusedError / requests.exceptions.ConnectionError）。用 try-except 捕获并提示用户。
挑战 6.2: 自己写一个 FastAPI 后端返回 {"answer": "这是一个模拟回答"}，前端调通。这是 Day39-40 的预演。
"""


# ============================================================
# 📌 今日 LeetCode 推荐
# ============================================================
#
# 1. LeetCode 1 - 两数之和（Easy）
#    链接：https://leetcode.cn/problems/two-sum/
#    思路提示：哈希表存"值→下标"，遍历时查 target - x。
#    今天学前端交互，可顺手做这道题保持手感。
#
# 2. LeetCode 20 - 有效的括号（Easy）
#    链接：https://leetcode.cn/problems/valid-parentheses/
#    思路提示：栈。括号嵌套就像 Streamlit 的组件嵌套，先进后出。
# ============================================================


# ============================================================
# 学习记录
# ============================================================
"""
📝 Day 37 学习打卡

完成时间：____年____月____日

我完成了：
[ ] 实验 1：文本与标题
[ ] 实验 2：文本输入交互
[ ] 实验 3：滑块/下拉/复选框
[ ] 实验 4：反馈消息
[ ] 实验 5：展示数据
[ ] 实验 6：Streamlit 调 FastAPI

遇到的问题：
_____________________________________________
_____________________________________________

学到的最重要的一点：
_____________________________________________
"""
